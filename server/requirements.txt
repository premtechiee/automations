# Server-only deps for the FastAPI companion (server/app.py).
# Install alongside the base requirements:
#     pip install -r requirements.txt -r server/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.5
PyJWT>=2.8
cryptography>=42.0
