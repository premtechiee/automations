# Runtime data directory — git-tracked placeholder
# Generated files (logs, prediction model, alert state, images) are stored here.
# Add the following patterns to .gitignore to avoid committing sensitive data:
#   data/*.json
#   data/*.png
#   data/*.log
